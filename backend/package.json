{
  "name": "rizipt-v2-api",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init:local": "wrangler d1 execute rizipt_v2 --file=./schema.sql --local",
    "db:init:remote": "wrangler d1 execute rizipt_v2 --file=./schema.sql --remote"
  },
  "dependencies": {
    "hono": "^4.6.9"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260901.1",
    "typescript": "^5.9.3",
    "wrangler": "^4.127.1"
  }
}
